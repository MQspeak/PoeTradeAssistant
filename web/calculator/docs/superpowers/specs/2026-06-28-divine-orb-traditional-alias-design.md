# 神圣石繁体别名兼容设计

## 目标

价格表导入时，将 `神聖石`、`神圣石` 和 `D` 视为同一个本地币种 `D`。

## 设计

- 在现有 `SCANNER_CURRENCY_ALIASES` 中加入 `神聖石 -> D`。
- 保留已有 `神圣石 -> D` 和 `D -> D` 映射。
- 继续复用现有币种查找、缺失币种创建、交易对创建和交易模式切换流程。
- 不修改买入比例、卖出比例或当前交易对比例的解析规则。

## 验证

- 导入测试必须覆盖 `神聖石`、`神圣石` 和 `D` 均映射为 `D`。
- 运行现有导入回归测试和项目构建。
