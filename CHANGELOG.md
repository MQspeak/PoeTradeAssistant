# 更新日志

## 2026-09-07

- 将截至 aed0dc3 的实时搜索、离线简中翻译、浏览器隐藏与恢复、计算器展示及排序功能纳入 main 基准。
- 清理已合并的两个本地开发分支，移除 34 个旧 Web 实现、历史扫描器设计和已完成预研文件；历史内容可从 aed0dc3 及此前提交恢复。
- SDD 更新至 1.2，同步 README 和实时搜索说明，明确浏览器进程、存储、发布依赖及尚未完成的功能。
- 验证：85 项自动化测试通过（Core 14、Integration 2、App 69）；Release 构建成功，0 警告、0 错误。本次未进行真实游戏、浏览器冒烟或发布包验证。
- 远端 fetch 因连接重置失败，本次基准整理仅涉及本地仓库，未推送。

## 2026-09-04

### 新增
- 扫描最高/最低买入价和最高/最低卖出价，按游戏通货位置读取比例分量。
- 支持高买低卖、高买高卖、低买高卖、低买低卖四种盈利模式，切换后立即重算并支持保存恢复。
- V2 合约增加四价及逐字段 OCR 状态；部分报价失败仍保留其他成功价格，缺少当前模式所需报价时显示“待计算”。
- 关闭窗口时提交编辑、停止扫描并等待导入，保存本次会话已加载的 POE1/POE2 工作区与应用设置；保存失败保留窗口供重试。

### 改进与修复
- 优先复用工作区和本批扫描已记录的金币成本，空金币结果不覆盖已有成本。
- 对调读取后恢复通货位置，取消操作时释放键鼠；对调失败停止扫描。
- profile、计算器工作区和应用设置改用临时文件刷新后替换，降低单文件半写风险。
- 调整原生界面、表格及下拉控件；重置扫描配置前增加确认。ROI 与金币效率统一保留两位小数。
- 修复 V2 单文件输出后自动导入仍拼接第二个 .v2 后缀的问题，并同步输出与四价回归测试。

### 输出与发布变更
- 每批仅生成一个时间戳 .v2.json，不再生成旧中文字段 JSON；依赖旧输出文件的外部流程需要调整。适配层保留旧 V1 导入能力。
- 发布脚本自动查找符合 global.json 的 .NET SDK，SDK 不可用时保留已有发布产物，并检查发布目录和正在运行的程序。
- 仅生成自包含发布目录，不再生成 ZIP；发布成功自动启动程序。分发时需复制整个目录。
- 更新 SDD 至文档版本 1.1，并同步 V2 数据合约说明。

### 验证
- 67 项自动化测试全部通过（Core 14、Integration 2、App 51）；Release 构建成功，0 警告、0 错误。
- 本次未进行真实游戏窗口 OCR、完整 WPF 交互或发布包启动验证。
